<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Search for items</h1>
    <%= link_to 'Create auction', new_auction_item_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <div class='dark:bg-gray-700 bg-white rounded-xl p-8 my-8 shadow'>
    <%= search_form(@search) %>
  </div>

  <%= table(@auction_items) %>
</div>
